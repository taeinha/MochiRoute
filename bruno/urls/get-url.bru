meta {
  name: Get URL
  type: http
  seq: 2
}

get {
  url: {{apiUrl}}/url/{{urlId}}
  body: none
  auth: none
}

docs {
  Requires auth_token cookie from Login or Register.
}

tests {
  test("should return 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
