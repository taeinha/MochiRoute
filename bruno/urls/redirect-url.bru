meta {
  name: Redirect URL
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/r/{{shortCode}}
  body: none
  auth: none
}

tests {
  test("should redirect", function() {
    expect(res.getStatus()).to.be.oneOf([301, 302, 307, 308]);
  });
}
